# Superseded prototype notes

The 20 fictional examples were replaced by the researched player-season import.
See [Player database research](PLAYER_DATABASE_RESEARCH.md), the root README,
and `data/coverage_summary.json` for current coverage and limitations.
The older `college-team-source-index.json` is retained as the original incomplete
source discovery snapshot; it is not the historical membership source.
